# Core Dependencies
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.0
deepspeed>=0.12.0

# Dataset Preparation
pyyaml>=6.0
tqdm>=4.65.0
datasets>=2.14.0

# Optimization
flash-attn>=2.3.0

# Monitoring
tensorboard

# --- TPU (XLA) environment notes ---
# Do NOT install deepspeed/flash-attn on TPU. Instead install torch-xla
# with versions matching torch/torchvision on your TPU VM, e.g.:
# pip install torch==2.5.1 torchvision==0.20.1
# pip install torch-xla[tpu]==2.5.1
